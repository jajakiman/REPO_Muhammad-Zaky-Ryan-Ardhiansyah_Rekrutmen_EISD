@extends('layouts.dashboard')
@section('title', 'Kelola Kategori Masalah | AksesLoka')
@section('content')<section class="admin-section"><div class="container"><div class="page-heading"><div><p class="eyebrow">Master data</p><h1>Kategori masalah</h1></div><a class="button button-primary" href="{{ route('admin.issue-categories.create') }}">Tambah kategori</a></div>
@if($categories->isEmpty())<div class="empty-state"><h2>Belum ada kategori</h2><p>Tambahkan kategori masalah pertama.</p></div>@else<div class="table-wrap"><table><caption class="sr-only">Daftar kategori masalah</caption><thead><tr><th scope="col">Nama</th><th scope="col">Status</th><th scope="col">Aksi</th></tr></thead><tbody>@foreach($categories as $category)<tr><th scope="row" data-label="Nama">{{ $category->name }}</th><td data-label="Status"><span class="badge {{ $category->is_active ? 'badge-positive' : 'badge-neutral' }}">{{ $category->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td data-label="Aksi"><div class="table-actions"><a href="{{ route('admin.issue-categories.edit',$category) }}">Ubah</a>@if($category->is_active)<form method="post" action="{{ route('admin.issue-categories.deactivate',$category) }}">@csrf @method('patch')<button class="link-button" type="submit">Nonaktifkan</button></form>@endif</div></td></tr>@endforeach</tbody></table></div>@endif</div></section>@endsection
