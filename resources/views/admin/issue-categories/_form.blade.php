@csrf @isset($issueCategory) @method('put') @endisset
<div class="field"><label for="name">Nama kategori</label><input id="name" name="name" value="{{ old('name', $issueCategory->name ?? '') }}" required aria-describedby="name-error">@error('name')<p class="field-error" id="name-error">{{ $message }}</p>@enderror</div>
@isset($issueCategory)<div class="field"><label for="is_active">Status</label><select id="is_active" name="is_active" required><option value="1" @selected((string)old('is_active',(int)$issueCategory->is_active)==='1')>Aktif</option><option value="0" @selected((string)old('is_active',(int)$issueCategory->is_active)==='0')>Nonaktif</option></select>@error('is_active')<p class="field-error">{{ $message }}</p>@enderror</div>@endisset
<div class="actions"><button class="button button-primary" type="submit">Simpan kategori</button><a class="button button-secondary" href="{{ route('admin.issue-categories.index') }}">Batal</a></div>
